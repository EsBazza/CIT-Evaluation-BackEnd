services:
  - type: web
    name: cit-eval-backend
    runtime: java
    plan: free
    buildCommand: chmod +x mvnw && ./mvnw -DskipTests clean package
    startCommand: java -Dserver.port=$PORT -jar target/cit-eval-0.0.1-SNAPSHOT.jar
    healthCheckPath: /api/keys/handshake